# GPU requirements — torch/torchvision not pinned, use pre-installed versions (e.g. RunPod images)
# For macOS, torch/torchvision are included without CUDA builds

torch; sys_platform == 'darwin'
torchvision; sys_platform == 'darwin'

numpy>=1.24.2
opencv-python>=4.8.0
onnx>=1.14.0
insightface>=0.7.3
psutil>=5.9.5
pillow>=9.5.0
onnxruntime==1.15.0; sys_platform == 'darwin' and platform_machine != 'arm64'
onnxruntime-silicon==1.13.1; sys_platform == 'darwin' and platform_machine == 'arm64'
onnxruntime-gpu>=1.15.0; sys_platform != 'darwin'
tensorflow>=2.12.0
opennsfw2==0.10.2
protobuf>=3.20.0
tqdm>=4.65.0
gfpgan==1.3.8
websockets>=12.0
python-dotenv>=1.0.0
