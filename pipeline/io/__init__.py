"""
I/O abstraction layer for the Phantom pipeline.

Provides input/output abstractions for flexible frame source and sink handling.

Modules:
- capture.py: Input sources (webcam, network, file, image sequence)
- output.py: Output sinks (file, HTTP, WebSocket)
- ffmpeg.py: FFmpeg utilities for video processing
"""
