"""
ML/CV services for the Phantom pipeline.

Extracted, standalone components with no global state (except internal caching).
Each service takes config and dependencies as constructor arguments.
"""
